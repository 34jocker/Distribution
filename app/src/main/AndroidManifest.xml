<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jzbwlkj.distribution">

    <application
        android:name=".mvp.model.base.DistributionApplaction"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".mvp.views.activity.FindPassActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.views.base.BaseActivity" />
        <!-- <activity android:name=".views.activity.LoginActivity"></activity> -->
        <activity android:name=".mvp.views.activity.RegisterActivity" />
        <!-- <activity android:name=".views.activity.FindPassActivity"></activity> -->
        <activity android:name=".mvp.views.activity.RetrievePass" />
    </application>

</manifest>